#include <bits/stdc++.h>
using namespace std;

int main () {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    rep(i, 0, n) {
      cin >> arr[i];
    }
    sort(arr, arr + n);
    rep (i, 0, n) {
    }
  }
  return 0;
}

sum(0, z-1) + (n-z) * arr[z-1]
